export const ImageUrl = path => {
  return `${window.location.origin}/assets/images/${path}`;
};

export const VideoUrl = path => {
    return `${window.location.origin}/assets/video/${path}`;
  };
  
